package com.Algorithm.Sort;

/***
 * 堆排序
 * 使用二叉树模式
 *
 * 包括大根堆，小根堆
 *
 * 堆是一种完全二叉树，大根堆，是每轮迭代之后，根节点是最大数，和最末尾的叶子节点进行置换。
 * 堆排序的平均时间复杂度为 Ο(nlogn)。
 *
 * 算法流程：
 *
 *
 */
public class HeapSort {
    public static void main(String[] args) {


    }

    public int[] heapSord(int[] data){
        int len = data.length;
        // 初始化数组

        // 构建大根堆

        // 不断置换元素，不断调整堆，直到堆中只保留一个元素。

        return data;
    }

    /**
     * 调整堆中元素的具体位置
     * int[] data：原始数据
     * int len：待调整的数组总长度
     * int index：待调整元素下标
     * */
    private void heapAdjust(int[] data,int len,int index){
//        if ()
    }
}
